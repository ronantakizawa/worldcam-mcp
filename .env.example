# Optional API keys - sources degrade gracefully without them
WINDY_API_KEY=
OPENWEBCAMDB_API_KEY=
HELIOS_CLIENT_ID=
HELIOS_CLIENT_SECRET=

# Optional: default directory for saving screenshots
WORLDCAM_SAVE_DIR=

# Optional: ffmpeg path override (defaults to 'ffmpeg' in PATH)
FFMPEG_PATH=
